import React from 'react';
import { fireEvent, render } from "@testing-library/react";
import App from "../App";


describe('Esto es la primera prueba ', () => {
  test('esta test esta de ejemplo por eso esta comentada',()=>{})
/*    render(<App />);


  test('probando comonente boton mas<App />', () => {

    //render(<App />);

    //expect(screen.debug()).toMatchSnapshot();
    const el=document.querySelector(".buttonADD");
    if(el){

      fireEvent.click(el);
      expect(document.querySelector('.text-counter')?.innerHTML).toBe("1");
    }

  });


  test('probando comonente boton menos <App />', () => {

    //render(<App />);

    //expect(screen.debug()).toMatchSnapshot();
    const el=document.querySelector(".buttonmmenos");
    if(el){

      fireEvent.click(el);
      fireEvent.click(el);
      expect(document.querySelector('.text-counter')?.innerHTML).toBe("-2");
    }

  });  */


});
